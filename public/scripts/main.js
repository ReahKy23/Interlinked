const maxChecked = 1;
let selectedCategories = 0;

//function that checks if the number of categories checked is greater than the max allowed, if so, uncheck the box and alert user
document.querySelectorAll("")